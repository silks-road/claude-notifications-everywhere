# Claude Notifications — browser extension

Desktop notifications when Claude finishes or needs you in **claude.ai** (browser), using the same notification pipeline (titles, four sounds, click-to-open) as the rest of `claude-vivre-card`.

## How it works

```
claude.ai tab                            your Mac
┌──────────────────────────┐   POST     ┌───────────────────────────┐
│ background.js observes    │  localhost │ claude-notifications serve │
│ the response stream ending│ ─────────▶ │  (LaunchAgent, port 52741) │
│ (network-level, immune to │  + token   │  → notification pipeline   │
│ UI redesigns);            │            └───────────────────────────┘
│ content.js supplies title │
│ + last message text       │
└──────────────────────────┘
```

The toolbar icon flashes a **green ✓** each time a finished turn is detected (and a **red E** if the local listener could not be reached) — so you can always see the pipeline working.

The extension only talks to `127.0.0.1` on your own machine; a shared token (generated by the installer) authenticates it. Nothing leaves your computer.

## Setup (macOS)

1. **Start the local listener** — in a terminal, or paste into a Claude chat and let Claude run it:
   ```
   claude-notifications install-browser-listener
   ```
   It prints a **token** and installs a LaunchAgent that keeps the listener running.

2. **Load the extension in Chrome:**
   - Go to `chrome://extensions`
   - Turn on **Developer mode** (top-right)
   - Click **Load unpacked** and select this `extension/` folder

3. **Paste the token:** click the extension's icon → paste the token → **Save & test**. You should get a test notification.

Done — now every claude.ai turn notifies you, and clicking the notification opens that exact chat.

## Notes

- **Receive + click only.** Browser notifications open the chat on click; there are no approval buttons here (those are macOS-app-specific).
- **Privacy:** the listener binds to loopback and requires the token; the extension's host permissions are limited to `claude.ai` and the local port.
- **Not on the Chrome Web Store yet** — this is the unpacked/developer install. A Web Store listing may come later.

## Troubleshooting

| Symptom | Fix |
|---|---|
| No ✓ flash when a reply finishes | Reload the extension card in `chrome://extensions`, then reload the claude.ai tab |
| ✓ flashes but no notification (red E) | The local listener is down — run `claude-notifications install-browser-listener` again, or check `~/.claude/browser-listener.log` |
| Popup says "Token rejected (401)" | Re-copy the token printed by the installer (`~/.claude/browser-listener-token`) |
| Notification body is generic ("Done") with no text | Reload the claude.ai tab — the page-reader script only enters pages loaded after the extension |
